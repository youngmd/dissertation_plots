model{
  for (i in 1:N) {
    y[i] ~ dnorm(mu[i], tau)
    eta[i] <- beta.0 + beta.1 * x[i]
    log(mu[i])<-log(exp(eta[i]))
  }
  beta.0 ~ dnorm(0, .0001)
  beta.1 ~ dnorm(0, .0001)
  tau <- pow(sigma, -2)
  sigma ~ dunif(0, 100)

}